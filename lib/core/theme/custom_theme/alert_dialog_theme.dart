import 'package:flutter/material.dart';

class AlertDialogTheme {
  AlertDialogTheme._();

  static final lightAlertDialogTheme = DialogTheme(
    backgroundColor: Colors.white,
    shape: RoundedRectangleBorder(
      borderRadius: BorderRadius.circular(8),
    ),
  );
}
